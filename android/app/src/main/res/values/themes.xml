<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Only what shows before Compose draws (and the Android 12+ splash). The
         real theme is PcRemoteTheme in Compose. -->
    <style name="Theme.PcRemote" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
    </style>
</resources>
