<?xml version="1.0" encoding="utf-8"?>
<!--
  Android 12+ ignores allowBackup="false" for device-to-device transfers. The
  credentials are encrypted with a Keystore key that never leaves the phone, so a
  copied file would be useless on the new device — but there is no reason to copy
  it at all. Exclude everything from both cloud backup and device transfer.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
    </device-transfer>
</data-extraction-rules>
